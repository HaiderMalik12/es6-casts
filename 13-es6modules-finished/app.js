import {
    apiKey as key,
    host,
    greet,
    address,
    london
} from './src/config';
import person from './src/person';
console.log(key, host);
greet('John')
console.log(person)
console.log(address, london);